- name: Install Sys Packages 1.0 (curl, fzf, gettext, python3-pip, ripgrep)
  become: true
  apt:
    name: ["curl", "fzf", "gettext", "python3-pip", "ripgrep"]
  tags:
    - install
    - core
- name: Install Sys Programs 1.0 (i3, tmux)
  become: true
  apt:
    name: ["i3", "tmux"]
  tags:
    - install
    - core
