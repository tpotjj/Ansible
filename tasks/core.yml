- name: Install Sys Packages 1.0 (curl, feh, fzf, gettext, lsb-release, python3-pip, ripgrep)
  become: true
  apt:
    name: ["curl", "feh", "fzf", "gettext", "lsb-release", "python3-pip", "ripgrep"]
  tags:
    - install
    - core
    - redis
- name: Install Sys Programs 1.0 (arandr, guvcview, i3, pavucontrol, tmux)
  become: true
  apt:
    name: ["arandr", "guvcview", "i3", "pavucontrol", "tmux"]
  tags:
    - install
    - core
